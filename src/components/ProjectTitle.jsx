import React from 'react';

const ProjectTitle = ({ text }) => {
  return (
    <h2 className="text-[1.75rem] text-darkGreen font-semibold font-['Work Sans']">
      {text}
    </h2>
  );
};

export default ProjectTitle;
